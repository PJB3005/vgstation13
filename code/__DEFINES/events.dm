// standardized defines for event arguments.

#define EVENT_AMOUNT     "amount"
#define EVENT_OLD_AMOUNT "old_amount"

// Ways of adjusting (think the 3 different procs to stun somebody, adjust, add, set)
#define EVENT_ADJUST_TYPE   "adjust_type"

#define EVENT_ADJUST_ADD    "add"
#define EVENT_ADJUST_ADJUST "adjust"
#define EVENT_ADJUST_SET    "set"
